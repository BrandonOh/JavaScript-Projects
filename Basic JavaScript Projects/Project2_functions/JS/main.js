function add(){
    var c = "cats "; //create a string variable.
    c += " are furry." //concatenate the variable with a string.
    document.getElementById("text").innerHTML = c; //replace the text in the element id.
}